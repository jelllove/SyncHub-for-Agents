name: gemini
config:
  paths:
    windows: "%USERPROFILE%\\.gemini"
    darwin: "~/.gemini"
    linux: "~/.gemini"
  include:
    - "settings.json"
    - "projects.json"
    - "state.json"
    - "trustedFolders.json"
  sessions:
    - "sessions/**/*.json"
    - "tmp/**/chats/**/*.jsonl"
  exclude:
    - "**/*token*"
    - "**/oauth_creds.json"
secrets:
  key_patterns: ["apiKey", "token", "secret", "password", "oauth", "refresh_token"]
